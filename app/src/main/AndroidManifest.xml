<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.etecia.menuappetecia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Sobre_Activity"></activity>
        <activity android:name=".Salvos_Activity" />
        <activity android:name=".Pesquisar_Activity" />
        <activity android:name=".Compartilhar_Activity" />
        <activity android:name=".Favoritos_Activity" />
        <activity android:name=".Configuracoes_Activity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/TemaSemBarra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>